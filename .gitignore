# Dependencies and generated local output
node_modules/
coverage/
build/
.build/
dist-v2/
*.log
.DS_Store
server.js.bak-*
server.js.before-*

# Runtime and deployment secrets
.env*
!.env.example
.grant-secret
secrets/
/etc/bittybox/
.hermes*

# Production user data and local-only state
data/
.firebase/
.netlify/

# Frontend local state
frontend/node_modules/
frontend/.env*
frontend/.netlify/

# dist is a LOCAL build-output symlink (-> .build). It is NOT the live docroot.
# It was previously -> /var/www/bittybox.org/docs, which made a bare `vite build`
# publish straight to production. Never repoint it at a served directory.
!dist
